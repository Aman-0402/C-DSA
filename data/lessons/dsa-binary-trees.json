{
  "id": "dsa-binary-trees",
  "moduleId": "trees",
  "title": "Binary Trees",
  "description": "Formalizing the two-children-per-node tree from Trees: the full/complete/perfect/balanced vocabulary, and counting nodes recursively.",
  "objectives": [
    "Define a binary tree precisely",
    "Distinguish full, complete, perfect, and balanced binary trees",
    "Build a small binary tree by hand",
    "Count a binary tree's nodes recursively"
  ],
  "sections": [
    {
      "type": "heading",
      "text": "1. Recap: We Already Used This Shape"
    },
    {
      "type": "paragraph",
      "text": "Trees' TreeNode already had exactly left and right — that IS a binary tree. Now let's define it formally and learn its vocabulary."
    },
    {
      "type": "heading",
      "text": "2. Formal Definition"
    },
    {
      "type": "definition",
      "term": "Binary tree",
      "text": "A binary tree is a tree in which every node has at most two children, conventionally distinguished as left and right."
    },
    {
      "type": "heading",
      "text": "3. Types of Binary Trees"
    },
    {
      "type": "table",
      "headers": ["Type", "Definition"],
      "rows": [
        ["Full", "Every node has either 0 or 2 children — never exactly 1"],
        ["Complete", "Every level is fully filled except possibly the last, which fills left to right (recall Heap's array-based tree — this is exactly that shape)"],
        ["Perfect", "Every internal node has exactly 2 children, AND every leaf is at the same depth"],
        ["Balanced", "The height of the two subtrees of any node never differs by more than some small bound (commonly 1) — kept roughly even"]
      ]
    },
    {
      "type": "note",
      "text": "These aren't mutually exclusive — a tree can be both full and balanced, for instance. \"Balanced\" specifically will matter a great deal once we reach Binary Search Trees, where an unbalanced tree can quietly ruin performance."
    },
    {
      "type": "heading",
      "text": "4. Building a Simple Binary Tree"
    },
    {
      "type": "code",
      "lang": "cpp",
      "code": "struct TreeNode\n{\n    int data;\n    TreeNode *left;\n    TreeNode *right;\n};\n\nTreeNode *root = new TreeNode{1, nullptr, nullptr};\nroot->left = new TreeNode{2, nullptr, nullptr};\nroot->right = new TreeNode{3, nullptr, nullptr};\nroot->left->left = new TreeNode{4, nullptr, nullptr};\nroot->left->right = new TreeNode{5, nullptr, nullptr};"
    },
    {
      "type": "note",
      "text": "This builds the same shape used to compute height in Trees, extended with one more node — a small binary tree, five nodes, built entirely with new (recall Dynamic Memory Allocation) and pointer assignment."
    },
    {
      "type": "heading",
      "text": "5. Counting Nodes Recursively"
    },
    {
      "type": "code",
      "lang": "cpp",
      "filename": "main.cpp",
      "code": "#include <iostream>\n\nusing namespace std;\n\nstruct TreeNode\n{\n    int data;\n    TreeNode *left;\n    TreeNode *right;\n};\n\nint countNodes(TreeNode *node)\n{\n    if (node == nullptr) // BASE CASE\n    {\n        return 0;\n    }\n    return 1 + countNodes(node->left) + countNodes(node->right); // RECURSIVE CASE\n}\n\nint main()\n{\n    TreeNode *root = new TreeNode{1, nullptr, nullptr};\n    root->left = new TreeNode{2, nullptr, nullptr};\n    root->right = new TreeNode{3, nullptr, nullptr};\n    root->left->left = new TreeNode{4, nullptr, nullptr};\n    root->left->right = new TreeNode{5, nullptr, nullptr};\n\n    cout << countNodes(root) << endl;\n\n    return 0;\n}",
      "output": "5",
      "retypePractice": true,
      "retypeId": "dsa-binary-trees-count"
    },
    {
      "type": "keyPoints",
      "title": "Tracing it",
      "points": [
        "countNodes(4) = 1 + countNodes(nullptr) + countNodes(nullptr) = 1 + 0 + 0 = 1",
        "countNodes(5) = 1 (same reasoning, a leaf)",
        "countNodes(2) = 1 + countNodes(4)=1 + countNodes(5)=1 = 3",
        "countNodes(3) = 1 + 0 + 0 = 1",
        "countNodes(1) = 1 + countNodes(2)=3 + countNodes(3)=1 = 5 — the total"
      ]
    },
    {
      "type": "note",
      "text": "This is the exact same recursive shape as height() from Trees — a base case for nullptr, and a recursive case combining both children's results. Most binary tree algorithms follow this identical template, just changing what gets combined and how."
    },
    {
      "type": "heading",
      "text": "6. Real-Life Example"
    },
    {
      "type": "realLifeExample",
      "title": "A single-elimination tournament bracket, always exactly two competitors per match",
      "text": "A tournament bracket where every match has exactly two competitors (never one, never three) is a full binary tree in spirit — each match (node) either produces two child matches or is itself a final, leaf-level result. Counting how many total matches occurred is exactly countNodes(): count this match, plus everything in the left half of the bracket, plus everything in the right half."
    },
    {
      "type": "commonMistakes",
      "title": "Common Beginner Mistakes",
      "items": [
        "Confusing complete and perfect binary trees — complete only requires the last level to fill left-to-right; perfect requires every leaf at the exact same depth.",
        "Forgetting the base case (node == nullptr) in a recursive tree function — without it, the recursion never terminates.",
        "Assuming a binary tree is automatically balanced — balance is a separate property that must be actively maintained or checked, not guaranteed just by being binary.",
        "Writing a loop-based node counter when the recursive version is both simpler and more directly matches the tree's own recursive structure."
      ]
    },
    {
      "type": "interviewQuestions",
      "title": "Interview Questions",
      "questions": [
        {
          "question": "What is a binary tree?",
          "answer": "A tree in which every node has at most two children, conventionally called left and right."
        },
        {
          "question": "What is the difference between a complete and a perfect binary tree?",
          "answer": "A complete binary tree fills every level fully except possibly the last, which fills left to right; a perfect binary tree requires every internal node to have exactly two children and every leaf to be at the same depth."
        },
        {
          "question": "What does 'balanced' mean for a binary tree?",
          "answer": "The height of the two subtrees of any node never differs by more than a small bound (commonly 1), keeping the tree roughly even rather than lopsided."
        },
        {
          "question": "How would you count the total number of nodes in a binary tree?",
          "answer": "Recursively: a nullptr node contributes 0, and any real node contributes 1 plus the node counts of its left and right subtrees."
        }
      ],
      "thirtySecondAnswer": "A binary tree is a tree where every node has at most two children, left and right. Several named shapes matter: full (every node has 0 or 2 children, never 1), complete (every level full except possibly the last, filled left to right — the shape Heap relies on), perfect (every leaf at the same depth), and balanced (subtree heights never differ by more than a small bound), which becomes especially important once we reach Binary Search Trees. Counting a binary tree's nodes follows the same recursive template as computing its height: a nullptr contributes 0, and any real node contributes 1 plus the counts of its left and right subtrees."
    },
    {
      "type": "memoryTrick",
      "title": "Trick to Remember Binary Trees",
      "text": "Binary tree = a tournament bracket, always 0 or 2 per match. Full = never exactly 1 child. Complete = last row fills left to right. Perfect = every leaf same depth. Balanced = neither side towers over the other."
    },
    {
      "type": "heading",
      "text": "LeetCode: Binary Trees in Practice"
    },
    {
      "type": "leetcode",
      "id": "lc-104",
      "number": 104,
      "title": "Maximum Depth of Binary Tree",
      "difficulty": "Easy",
      "link": "https://leetcode.com/problems/maximum-depth-of-binary-tree/",
      "problemStatement": "Given the root of a binary tree, return its maximum depth (the number of nodes along the longest path from root to a leaf).",
      "examples": [
        { "input": "root = [3,9,20,null,null,15,7]", "output": "3" }
      ],
      "constraints": ["0 <= number of nodes <= 10^4"],
      "hints": [
        "This is the exact same recursive shape as this lesson's countNodes() — a nullptr contributes a base value, and a real node combines its children's results.",
        "The depth of a node is 1 (itself) plus the LARGER of its two subtrees' depths, not the sum.",
        "Base case: an empty tree (nullptr) has depth 0."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Level-order BFS, counting how many full levels are processed.",
          "code": "#include <iostream>\n#include <queue>\n\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nint maxDepth(TreeNode* root)\n{\n    if (root == nullptr) return 0;\n\n    int depth = 0;\n    queue<TreeNode*> q;\n    q.push(root);\n\n    while (!q.empty())\n    {\n        int size = q.size();\n        for (int i = 0; i < size; i++)\n        {\n            TreeNode* node = q.front(); q.pop();\n            if (node->left) q.push(node->left);\n            if (node->right) q.push(node->right);\n        }\n        depth++;\n    }\n\n    return depth;\n}\n\nint main()\n{\n    TreeNode* root = new TreeNode(3);\n    root->left = new TreeNode(9);\n    root->right = new TreeNode(20);\n    root->right->left = new TreeNode(15);\n    root->right->right = new TreeNode(7);\n\n    cout << maxDepth(root) << endl;\n\n    return 0;\n}",
          "output": "3",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(n) for the queue"
        },
        {
          "label": "Optimal",
          "explanation": "A short recursive definition: 1 + the larger of the two children's depths.",
          "code": "#include <iostream>\n\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nint maxDepth(TreeNode* root)\n{\n    if (root == nullptr) return 0;\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n}\n\nint main()\n{\n    TreeNode* root = new TreeNode(3);\n    root->left = new TreeNode(9);\n    root->right = new TreeNode(20);\n    root->right->left = new TreeNode(15);\n    root->right->right = new TreeNode(7);\n\n    cout << maxDepth(root) << endl;\n\n    return 0;\n}",
          "output": "3",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(h) call stack, h = tree height"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-100",
      "number": 100,
      "title": "Same Tree",
      "difficulty": "Easy",
      "link": "https://leetcode.com/problems/same-tree/",
      "problemStatement": "Given the roots of two binary trees p and q, return true if they are structurally identical with the same node values.",
      "examples": [
        { "input": "p = [1,2,3], q = [1,2,3]", "output": "true" }
      ],
      "constraints": ["0 <= number of nodes <= 100"],
      "hints": [
        "Two trees match only if BOTH are empty at the same spots, or BOTH have a node with the same value AND matching left/right subtrees — a direct recursive comparison.",
        "Base cases: both nullptr → true (matching empty). Exactly one nullptr → false (structural mismatch).",
        "Recursive case: values must match, AND both left subtrees must match, AND both right subtrees must match."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Serialize both trees to strings (preorder with null markers) and compare the strings.",
          "code": "#include <iostream>\n#include <string>\n\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nvoid serialize(TreeNode* node, string &out)\n{\n    if (node == nullptr) { out += \"#,\"; return; }\n    out += to_string(node->val) + \",\";\n    serialize(node->left, out);\n    serialize(node->right, out);\n}\n\nbool isSameTree(TreeNode* p, TreeNode* q)\n{\n    string sp, sq;\n    serialize(p, sp);\n    serialize(q, sq);\n    return sp == sq;\n}\n\nint main()\n{\n    TreeNode* p = new TreeNode(1); p->left = new TreeNode(2); p->right = new TreeNode(3);\n    TreeNode* q = new TreeNode(1); q->left = new TreeNode(2); q->right = new TreeNode(3);\n\n    cout << (isSameTree(p, q) ? \"true\" : \"false\") << endl;\n\n    return 0;\n}",
          "output": "true",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(n) for the serialized strings"
        },
        {
          "label": "Optimal",
          "explanation": "Direct recursive comparison, no serialization needed.",
          "code": "#include <iostream>\n\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nbool isSameTree(TreeNode* p, TreeNode* q)\n{\n    if (p == nullptr && q == nullptr) return true;\n    if (p == nullptr || q == nullptr) return false;\n\n    return p->val == q->val && isSameTree(p->left, q->left) && isSameTree(p->right, q->right);\n}\n\nint main()\n{\n    TreeNode* p = new TreeNode(1); p->left = new TreeNode(2); p->right = new TreeNode(3);\n    TreeNode* q = new TreeNode(1); q->left = new TreeNode(2); q->right = new TreeNode(3);\n\n    cout << (isSameTree(p, q) ? \"true\" : \"false\") << endl;\n\n    return 0;\n}",
          "output": "true",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(h) call stack"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-226",
      "number": 226,
      "title": "Invert Binary Tree",
      "difficulty": "Easy",
      "link": "https://leetcode.com/problems/invert-binary-tree/",
      "problemStatement": "Given the root of a binary tree, invert it (swap every node's left and right children) and return the root.",
      "examples": [
        { "input": "root = [4,2,7,1,3,6,9]", "output": "[4,7,2,9,6,3,1]" }
      ],
      "constraints": ["0 <= number of nodes <= 100"],
      "hints": [
        "Every single node's left and right children need to be swapped — including at every level, not just the root.",
        "Recursively invert the left subtree, invert the right subtree, then swap the (now-inverted) results at the current node.",
        "Order doesn't matter much here: swap first then recurse into the (already-swapped) children, or recurse first then swap — both work as long as every node's children end up swapped."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Level-order BFS, swapping each node's children as it's visited.",
          "code": "#include <iostream>\n#include <queue>\n\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nTreeNode* invertTree(TreeNode* root)\n{\n    if (root == nullptr) return nullptr;\n\n    queue<TreeNode*> q;\n    q.push(root);\n\n    while (!q.empty())\n    {\n        TreeNode* node = q.front(); q.pop();\n        swap(node->left, node->right);\n\n        if (node->left) q.push(node->left);\n        if (node->right) q.push(node->right);\n    }\n\n    return root;\n}\n\nint main()\n{\n    TreeNode* root = new TreeNode(4);\n    root->left = new TreeNode(2);\n    root->right = new TreeNode(7);\n\n    TreeNode* result = invertTree(root);\n    cout << result->left->val << \" \" << result->right->val << endl;\n\n    return 0;\n}",
          "output": "7 2",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(n) for the queue"
        },
        {
          "label": "Optimal",
          "explanation": "A short recursive swap at every node.",
          "code": "#include <iostream>\n\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nTreeNode* invertTree(TreeNode* root)\n{\n    if (root == nullptr) return nullptr;\n\n    swap(root->left, root->right);\n    invertTree(root->left);\n    invertTree(root->right);\n\n    return root;\n}\n\nint main()\n{\n    TreeNode* root = new TreeNode(4);\n    root->left = new TreeNode(2);\n    root->right = new TreeNode(7);\n\n    TreeNode* result = invertTree(root);\n    cout << result->left->val << \" \" << result->right->val << endl;\n\n    return 0;\n}",
          "output": "7 2",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(h) call stack"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-101",
      "number": 101,
      "title": "Symmetric Tree",
      "difficulty": "Easy",
      "link": "https://leetcode.com/problems/symmetric-tree/",
      "problemStatement": "Given the root of a binary tree, return true if it is a mirror of itself around its center.",
      "examples": [
        { "input": "root = [1,2,2,3,4,4,3]", "output": "true" }
      ],
      "constraints": ["1 <= number of nodes <= 1000"],
      "hints": [
        "This is Same Tree's comparison idea, but comparing a tree against a MIRRORED version of itself rather than against a separate second tree.",
        "Write a helper isMirror(left, right) that checks: both nullptr, OR both have equal values AND left's left mirrors right's right AND left's right mirrors right's left.",
        "Call the helper on root->left and root->right — that's the entire check."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Collect a level-order traversal including null markers, then check if it reads the same forward and backward level by level.",
          "code": "#include <iostream>\n#include <queue>\n#include <vector>\n\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nbool isSymmetric(TreeNode* root)\n{\n    if (root == nullptr) return true;\n\n    queue<TreeNode*> q;\n    q.push(root->left);\n    q.push(root->right);\n\n    while (!q.empty())\n    {\n        TreeNode* a = q.front(); q.pop();\n        TreeNode* b = q.front(); q.pop();\n\n        if (a == nullptr && b == nullptr) continue;\n        if (a == nullptr || b == nullptr || a->val != b->val) return false;\n\n        q.push(a->left); q.push(b->right);\n        q.push(a->right); q.push(b->left);\n    }\n\n    return true;\n}\n\nint main()\n{\n    TreeNode* root = new TreeNode(1);\n    root->left = new TreeNode(2);\n    root->right = new TreeNode(2);\n    root->left->left = new TreeNode(3);\n    root->left->right = new TreeNode(4);\n    root->right->left = new TreeNode(4);\n    root->right->right = new TreeNode(3);\n\n    cout << (isSymmetric(root) ? \"true\" : \"false\") << endl;\n\n    return 0;\n}",
          "output": "true",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(n) for the queue"
        },
        {
          "label": "Optimal",
          "explanation": "A recursive mirror-check helper, comparing outer and inner pairs of children.",
          "code": "#include <iostream>\n\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nbool isMirror(TreeNode* left, TreeNode* right)\n{\n    if (left == nullptr && right == nullptr) return true;\n    if (left == nullptr || right == nullptr) return false;\n\n    return left->val == right->val\n        && isMirror(left->left, right->right)\n        && isMirror(left->right, right->left);\n}\n\nbool isSymmetric(TreeNode* root)\n{\n    if (root == nullptr) return true;\n    return isMirror(root->left, root->right);\n}\n\nint main()\n{\n    TreeNode* root = new TreeNode(1);\n    root->left = new TreeNode(2);\n    root->right = new TreeNode(2);\n    root->left->left = new TreeNode(3);\n    root->left->right = new TreeNode(4);\n    root->right->left = new TreeNode(4);\n    root->right->right = new TreeNode(3);\n\n    cout << (isSymmetric(root) ? \"true\" : \"false\") << endl;\n\n    return 0;\n}",
          "output": "true",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(h) call stack"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-543",
      "number": 543,
      "title": "Diameter of Binary Tree",
      "difficulty": "Easy",
      "link": "https://leetcode.com/problems/diameter-of-binary-tree/",
      "problemStatement": "Given the root of a binary tree, return the length (in edges) of the longest path between any two nodes — the path may or may not pass through the root.",
      "examples": [
        { "input": "root = [1,2,3,4,5]", "output": "3", "explanation": "The longest path is 4-2-1-3 (or 5-2-1-3), 3 edges." }
      ],
      "constraints": ["1 <= number of nodes <= 10^4"],
      "hints": [
        "The longest path THROUGH any given node equals that node's left subtree height plus its right subtree height — the diameter is the maximum of this sum across every node, not just the root.",
        "This means the height-computing recursion from this lesson's own countNodes()-style template needs one extra thing: updating a running 'best diameter so far' at every node as heights are computed.",
        "Compute height bottom-up (post-order): at each node, get left height and right height, update the answer with left+right, then return 1 + max(left, right) as this node's own height to its parent."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "For every node, separately compute its left and right subtree heights from scratch, tracking the best sum.",
          "code": "#include <iostream>\n\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nint height(TreeNode* node)\n{\n    if (node == nullptr) return 0;\n    return 1 + max(height(node->left), height(node->right));\n}\n\nint best = 0;\n\nvoid visit(TreeNode* node)\n{\n    if (node == nullptr) return;\n    best = max(best, height(node->left) + height(node->right));\n    visit(node->left);\n    visit(node->right);\n}\n\nint diameterOfBinaryTree(TreeNode* root)\n{\n    best = 0;\n    visit(root);\n    return best;\n}\n\nint main()\n{\n    TreeNode* root = new TreeNode(1);\n    root->left = new TreeNode(2);\n    root->right = new TreeNode(3);\n    root->left->left = new TreeNode(4);\n    root->left->right = new TreeNode(5);\n\n    cout << diameterOfBinaryTree(root) << endl;\n\n    return 0;\n}",
          "output": "3",
          "timeComplexity": "O(n²) — height() re-walks subtrees repeatedly",
          "spaceComplexity": "O(h) call stack"
        },
        {
          "label": "Optimal",
          "explanation": "A single post-order pass computes height AND updates the diameter simultaneously — no repeated subtree walks.",
          "code": "#include <iostream>\n\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nint best = 0;\n\nint height(TreeNode* node)\n{\n    if (node == nullptr) return 0;\n\n    int leftHeight = height(node->left);\n    int rightHeight = height(node->right);\n\n    best = max(best, leftHeight + rightHeight);\n\n    return 1 + max(leftHeight, rightHeight);\n}\n\nint diameterOfBinaryTree(TreeNode* root)\n{\n    best = 0;\n    height(root);\n    return best;\n}\n\nint main()\n{\n    TreeNode* root = new TreeNode(1);\n    root->left = new TreeNode(2);\n    root->right = new TreeNode(3);\n    root->left->left = new TreeNode(4);\n    root->left->right = new TreeNode(5);\n\n    cout << diameterOfBinaryTree(root) << endl;\n\n    return 0;\n}",
          "output": "3",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(h) call stack"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-110",
      "number": 110,
      "title": "Balanced Binary Tree",
      "difficulty": "Easy",
      "link": "https://leetcode.com/problems/balanced-binary-tree/",
      "problemStatement": "Given the root of a binary tree, return true if it is height-balanced — the left and right subtree heights of every node differ by at most 1.",
      "examples": [
        { "input": "root = [3,9,20,null,null,15,7]", "output": "true" }
      ],
      "constraints": ["0 <= number of nodes <= 5000"],
      "hints": [
        "This is this lesson's Balanced definition from Section 3, made checkable in code — every node's left and right subtree heights must differ by at most 1.",
        "The naive way recomputes height() separately at every node, which is wasteful — recall Diameter of Binary Tree's insight that height can be computed bottom-up in one pass while checking a condition simultaneously.",
        "An optimal version can use a sentinel value like -1 to mean 'already found unbalanced somewhere below' — once found, that -1 propagates straight up, short-circuiting all further work."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "At every node, recompute the height of its left and right subtrees from scratch and check the difference.",
          "code": "#include <iostream>\n#include <cstdlib>\n\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nint height(TreeNode* node)\n{\n    if (node == nullptr) return 0;\n    return 1 + max(height(node->left), height(node->right));\n}\n\nbool isBalanced(TreeNode* root)\n{\n    if (root == nullptr) return true;\n\n    int leftH = height(root->left);\n    int rightH = height(root->right);\n\n    if (abs(leftH - rightH) > 1) return false;\n\n    return isBalanced(root->left) && isBalanced(root->right);\n}\n\nint main()\n{\n    TreeNode* root = new TreeNode(3);\n    root->left = new TreeNode(9);\n    root->right = new TreeNode(20);\n    root->right->left = new TreeNode(15);\n    root->right->right = new TreeNode(7);\n\n    cout << (isBalanced(root) ? \"true\" : \"false\") << endl;\n\n    return 0;\n}",
          "output": "true",
          "timeComplexity": "O(n²) — height() re-walks subtrees at every node",
          "spaceComplexity": "O(h) call stack"
        },
        {
          "label": "Optimal",
          "explanation": "A single post-order pass computes height and detects imbalance together, using -1 as a sentinel to short-circuit once any imbalance is found below.",
          "code": "#include <iostream>\n#include <cstdlib>\n\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nint checkHeight(TreeNode* node)\n{\n    if (node == nullptr) return 0;\n\n    int leftH = checkHeight(node->left);\n    if (leftH == -1) return -1;\n\n    int rightH = checkHeight(node->right);\n    if (rightH == -1) return -1;\n\n    if (abs(leftH - rightH) > 1) return -1;\n\n    return 1 + max(leftH, rightH);\n}\n\nbool isBalanced(TreeNode* root)\n{\n    return checkHeight(root) != -1;\n}\n\nint main()\n{\n    TreeNode* root = new TreeNode(3);\n    root->left = new TreeNode(9);\n    root->right = new TreeNode(20);\n    root->right->left = new TreeNode(15);\n    root->right->right = new TreeNode(7);\n\n    cout << (isBalanced(root) ? \"true\" : \"false\") << endl;\n\n    return 0;\n}",
          "output": "true",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(h) call stack"
        }
      ]
    }
  ],
  "codeExamples": [
    { "id": "dsa-binary-trees-count", "lang": "cpp", "filename": "main.cpp", "code": "#include <iostream>\n\nusing namespace std;\n\nstruct TreeNode\n{\n    int data;\n    TreeNode *left;\n    TreeNode *right;\n};\n\nint countNodes(TreeNode *node)\n{\n    if (node == nullptr)\n    {\n        return 0;\n    }\n    return 1 + countNodes(node->left) + countNodes(node->right);\n}\n\nint main()\n{\n    TreeNode *root = new TreeNode{1, nullptr, nullptr};\n    root->left = new TreeNode{2, nullptr, nullptr};\n    root->right = new TreeNode{3, nullptr, nullptr};\n    root->left->left = new TreeNode{4, nullptr, nullptr};\n    root->left->right = new TreeNode{5, nullptr, nullptr};\n\n    cout << countNodes(root) << endl;\n\n    return 0;\n}", "output": "5" }
  ],
  "retypePractice": [
    {
      "id": "dsa-binary-trees-count",
      "reference": "#include <iostream>\n\nusing namespace std;\n\nstruct TreeNode\n{\n    int data;\n    TreeNode *left;\n    TreeNode *right;\n};\n\nint countNodes(TreeNode *node)\n{\n    if (node == nullptr)\n    {\n        return 0;\n    }\n    return 1 + countNodes(node->left) + countNodes(node->right);\n}\n\nint main()\n{\n    TreeNode *root = new TreeNode{1, nullptr, nullptr};\n    root->left = new TreeNode{2, nullptr, nullptr};\n    root->right = new TreeNode{3, nullptr, nullptr};\n    root->left->left = new TreeNode{4, nullptr, nullptr};\n    root->left->right = new TreeNode{5, nullptr, nullptr};\n\n    cout << countNodes(root) << endl;\n\n    return 0;\n}"
    }
  ],
  "practiceQuestions": [],
  "interviewQuestions": true,
  "leetcode": [],
  "completionRules": {
    "requireAllSectionsViewed": true,
    "requireRetypeIds": ["dsa-binary-trees-count"]
  }
}
