{
  "id": "dsa-binary-search",
  "moduleId": "searching-sorting",
  "title": "Binary Search",
  "description": "The O(log n) algorithm promised back in Big O Notation: cutting the search space in half with every step, on a sorted array.",
  "objectives": [
    "Implement binary search correctly",
    "Trace binary search step by step",
    "Understand why it achieves O(log n)",
    "Know binary search requires a sorted input"
  ],
  "sections": [
    {
      "type": "heading",
      "text": "1. Recap: This Was Promised in Big O Notation"
    },
    {
      "type": "paragraph",
      "text": "Big O Notation previewed O(log n) with: \"Binary Search... cuts the remaining problem roughly in half with each step.\" Here it is."
    },
    {
      "type": "heading",
      "text": "2. The Problem: Linear Search Is O(n)"
    },
    {
      "type": "code",
      "lang": "cpp",
      "code": "// Checking every element one by one\nfor (int i = 0; i < numbers.size(); i++)\n{\n    if (numbers[i] == target) { /* found at i */ }\n}"
    },
    {
      "type": "paragraph",
      "text": "For a sorted array specifically, checking every element wastes information — the sorted order itself tells us a lot about where the target could possibly be."
    },
    {
      "type": "heading",
      "text": "3. What is Binary Search?"
    },
    {
      "type": "definition",
      "term": "Binary search",
      "text": "Binary search finds a target in a sorted array by repeatedly checking the middle element and discarding the half of the remaining range that cannot contain the target."
    },
    {
      "type": "heading",
      "text": "4. Full Example"
    },
    {
      "type": "code",
      "lang": "cpp",
      "filename": "main.cpp",
      "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint binarySearch(vector<int> &numbers, int target)\n{\n    int left = 0;\n    int right = numbers.size() - 1;\n\n    while (left <= right)\n    {\n        int mid = left + (right - left) / 2;\n\n        if (numbers[mid] == target)\n        {\n            return mid;\n        }\n        else if (numbers[mid] < target)\n        {\n            left = mid + 1; // target must be in the RIGHT half\n        }\n        else\n        {\n            right = mid - 1; // target must be in the LEFT half\n        }\n    }\n    return -1; // not found\n}\n\nint main()\n{\n    vector<int> numbers = {2, 5, 8, 12, 16, 23, 38, 45};\n\n    cout << binarySearch(numbers, 23) << endl;\n\n    return 0;\n}",
      "output": "5",
      "retypePractice": true,
      "retypeId": "dsa-binary-search-basic"
    },
    {
      "type": "keyPoints",
      "title": "Tracing it (target = 23)",
      "points": [
        "left=0, right=7, mid=3 → numbers[3]=12, too small → left = 4",
        "left=4, right=7, mid=5 → numbers[5]=23, found! → return 5"
      ]
    },
    {
      "type": "warning",
      "text": "mid = left + (right - left) / 2 is deliberately written this way instead of (left + right) / 2 — for very large arrays, left + right could theoretically overflow int (recall Type Modifiers), while this form avoids that risk entirely."
    },
    {
      "type": "heading",
      "text": "5. Why O(log n): Halving Each Step"
    },
    {
      "type": "note",
      "text": "Every comparison eliminates half of whatever range remains. Starting with n elements: n → n/2 → n/4 → n/8 → ... → 1. The number of halvings needed to shrink n down to 1 is exactly log₂n — this is the direct source of binary search's O(log n) complexity, and exactly the shape previewed in Big O Notation."
    },
    {
      "type": "heading",
      "text": "6. The Requirement: Must Be Sorted"
    },
    {
      "type": "warning",
      "text": "Binary search's correctness depends entirely on the array being sorted — that's what guarantees \"if numbers[mid] is too small, the target can only be to the right\" is actually true. Running binary search on an unsorted array gives unreliable results, since that guarantee no longer holds."
    },
    {
      "type": "heading",
      "text": "7. Real-Life Example"
    },
    {
      "type": "realLifeExample",
      "title": "Looking up a word in a physical dictionary",
      "text": "Nobody looks up a word in a dictionary by reading every page from the start — you open somewhere in the middle, see whether your word comes before or after that page alphabetically, and jump to the middle of the correct remaining half. Repeating that a few times finds any word almost instantly, in a dictionary of any size — precisely binary search's logarithmic behavior."
    },
    {
      "type": "commonMistakes",
      "title": "Common Beginner Mistakes",
      "items": [
        "Running binary search on an unsorted array — the technique's correctness assumes sorted order.",
        "Using while (left < right) instead of while (left <= right), missing the case where left equals right (a single remaining candidate).",
        "Forgetting to update left = mid + 1 or right = mid - 1 — reusing mid itself in the next range causes an infinite loop.",
        "Writing (left + right) / 2 instead of left + (right - left) / 2 — a theoretical overflow risk for very large ranges."
      ]
    },
    {
      "type": "interviewQuestions",
      "title": "Interview Questions",
      "questions": [
        {
          "question": "What is binary search, and what does it require of its input?",
          "answer": "An algorithm that finds a target in a sorted array by repeatedly checking the middle element and discarding the half that cannot contain the target — it requires the array to be sorted."
        },
        {
          "question": "What is binary search's time complexity, and why?",
          "answer": "O(log n) — because each comparison halves the remaining search range, and it takes log₂n halvings to shrink n elements down to one."
        },
        {
          "question": "Why is mid = left + (right - left) / 2 preferred over (left + right) / 2?",
          "answer": "It avoids a theoretical integer overflow when left and right are both large, since (left + right) could exceed int's range before the division happens."
        }
      ],
      "thirtySecondAnswer": "Binary search finds a target in a sorted array by repeatedly checking the middle element and discarding half of the remaining range based on whether the target is smaller or larger — left = mid + 1 or right = mid - 1. Because each step halves the remaining search space, it takes only log₂n steps to narrow n elements down to one, giving O(log n) time, exactly the complexity class previewed in Big O Notation. Binary search's correctness depends entirely on the array being sorted; running it on unsorted data gives unreliable results."
    },
    {
      "type": "memoryTrick",
      "title": "Trick to Remember Binary Search",
      "text": "Binary search = looking up a word in a dictionary. Check the middle, discard the wrong half, repeat. Requires sorted order. O(log n) — halved every step."
    },
    {
      "type": "heading",
      "text": "LeetCode: Binary Search in Practice"
    },
    {
      "type": "leetcode",
      "id": "lc-704",
      "number": 704,
      "title": "Binary Search",
      "difficulty": "Easy",
      "link": "https://leetcode.com/problems/binary-search/",
      "problemStatement": "Given a sorted array of integers nums and a target, return the index of target, or -1 if not found.",
      "examples": [
        { "input": "nums = [-1,0,3,5,9,12], target = 9", "output": "4" }
      ],
      "constraints": ["1 <= nums.length <= 10^4", "nums sorted ascending"],
      "hints": [
        "This is the exact algorithm this lesson just taught — no variation needed.",
        "left/right pointers bracket the search range; mid splits it in half each iteration.",
        "target smaller than nums[mid] means search the left half; larger means search the right half."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Scan every element linearly.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint search(vector<int> &nums, int target)\n{\n    for (int i = 0; i < nums.size(); i++)\n    {\n        if (nums[i] == target) return i;\n    }\n    return -1;\n}\n\nint main()\n{\n    vector<int> nums = {-1, 0, 3, 5, 9, 12};\n\n    cout << search(nums, 9) << endl;\n\n    return 0;\n}",
          "output": "4",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(1)"
        },
        {
          "label": "Optimal",
          "explanation": "Standard binary search, halving the range each step.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint search(vector<int> &nums, int target)\n{\n    int left = 0, right = nums.size() - 1;\n\n    while (left <= right)\n    {\n        int mid = left + (right - left) / 2;\n        if (nums[mid] == target) return mid;\n        else if (nums[mid] < target) left = mid + 1;\n        else right = mid - 1;\n    }\n\n    return -1;\n}\n\nint main()\n{\n    vector<int> nums = {-1, 0, 3, 5, 9, 12};\n\n    cout << search(nums, 9) << endl;\n\n    return 0;\n}",
          "output": "4",
          "timeComplexity": "O(log n)",
          "spaceComplexity": "O(1)"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-35",
      "number": 35,
      "title": "Search Insert Position",
      "difficulty": "Easy",
      "link": "https://leetcode.com/problems/search-insert-position/",
      "problemStatement": "Given a sorted array nums and a target, return the index if found, or the index where it would be inserted to keep the array sorted.",
      "examples": [
        { "input": "nums = [1,3,5,6], target = 5", "output": "2" },
        { "input": "nums = [1,3,5,6], target = 2", "output": "1" },
        { "input": "nums = [1,3,5,6], target = 7", "output": "4" }
      ],
      "constraints": ["1 <= nums.length <= 10^4"],
      "hints": [
        "This is regular binary search, but instead of returning -1 on failure, something useful about WHERE the search ended is needed.",
        "When the loop ends (left > right), left has landed exactly at the position where target would need to be inserted — think about why.",
        "No special-casing is needed beyond returning left instead of -1."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Scan linearly, returning the first index where nums[i] >= target.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint searchInsert(vector<int> &nums, int target)\n{\n    for (int i = 0; i < nums.size(); i++)\n    {\n        if (nums[i] >= target) return i;\n    }\n    return nums.size();\n}\n\nint main()\n{\n    vector<int> nums = {1, 3, 5, 6};\n\n    cout << searchInsert(nums, 5) << endl;\n\n    return 0;\n}",
          "output": "2",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(1)"
        },
        {
          "label": "Optimal",
          "explanation": "Binary search, returning left when the target isn't found — left has converged exactly to the correct insertion index.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint searchInsert(vector<int> &nums, int target)\n{\n    int left = 0, right = nums.size() - 1;\n\n    while (left <= right)\n    {\n        int mid = left + (right - left) / 2;\n        if (nums[mid] == target) return mid;\n        else if (nums[mid] < target) left = mid + 1;\n        else right = mid - 1;\n    }\n\n    return left;\n}\n\nint main()\n{\n    vector<int> nums = {1, 3, 5, 6};\n\n    cout << searchInsert(nums, 5) << endl;\n\n    return 0;\n}",
          "output": "2",
          "timeComplexity": "O(log n)",
          "spaceComplexity": "O(1)"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-69",
      "number": 69,
      "title": "Sqrt(x)",
      "difficulty": "Easy",
      "link": "https://leetcode.com/problems/sqrtx/",
      "problemStatement": "Given a non-negative integer x, return the integer square root of x (the decimal part is truncated).",
      "examples": [
        { "input": "x = 8", "output": "2", "explanation": "sqrt(8) = 2.828..., truncated to 2." }
      ],
      "constraints": ["0 <= x <= 2^31 - 1"],
      "hints": [
        "This isn't binary search over an array — it's binary search over the ANSWER itself, a candidate value from 0 to x.",
        "For a candidate mid, checking mid*mid <= x tells you whether mid could still be the answer or a smaller one is needed — the same 'discard half' logic applies to a range of possible answers, not just an array.",
        "Track the largest mid found so far where mid*mid <= x — that's the floor square root once the search narrows to nothing left to check."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Try every integer from 0 upward until one squared exceeds x.",
          "code": "#include <iostream>\n\nusing namespace std;\n\nint mySqrt(int x)\n{\n    long i = 0;\n    while (i * i <= x) i++;\n    return i - 1;\n}\n\nint main()\n{\n    cout << mySqrt(8) << endl;\n\n    return 0;\n}",
          "output": "2",
          "timeComplexity": "O(√x)",
          "spaceComplexity": "O(1)"
        },
        {
          "label": "Optimal",
          "explanation": "Binary search on the answer range [0, x], keeping the largest candidate whose square doesn't exceed x.",
          "code": "#include <iostream>\n\nusing namespace std;\n\nint mySqrt(int x)\n{\n    long left = 0, right = x;\n    long answer = 0;\n\n    while (left <= right)\n    {\n        long mid = left + (right - left) / 2;\n\n        if (mid * mid <= x)\n        {\n            answer = mid;\n            left = mid + 1;\n        }\n        else\n        {\n            right = mid - 1;\n        }\n    }\n\n    return answer;\n}\n\nint main()\n{\n    cout << mySqrt(8) << endl;\n\n    return 0;\n}",
          "output": "2",
          "timeComplexity": "O(log x)",
          "spaceComplexity": "O(1)"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-367",
      "number": 367,
      "title": "Valid Perfect Square",
      "difficulty": "Easy",
      "link": "https://leetcode.com/problems/valid-perfect-square/",
      "problemStatement": "Given a positive integer num, return true if it is a perfect square.",
      "examples": [
        { "input": "num = 16", "output": "true" },
        { "input": "num = 14", "output": "false" }
      ],
      "constraints": ["1 <= num <= 2^31 - 1"],
      "hints": [
        "This is Sqrt(x)'s binary-search-on-the-answer idea again — search for a candidate whose square equals num exactly.",
        "Binary search the range [1, num], checking mid*mid against num.",
        "If the search ends without ever finding an exact match, num isn't a perfect square."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Try every integer from 1 upward, checking if its square equals num.",
          "code": "#include <iostream>\n\nusing namespace std;\n\nbool isPerfectSquare(int num)\n{\n    long i = 1;\n    while (i * i < num) i++;\n    return i * i == num;\n}\n\nint main()\n{\n    cout << (isPerfectSquare(16) ? \"true\" : \"false\") << endl;\n\n    return 0;\n}",
          "output": "true",
          "timeComplexity": "O(√num)",
          "spaceComplexity": "O(1)"
        },
        {
          "label": "Optimal",
          "explanation": "Binary search the range [1, num] for a candidate whose square exactly equals num.",
          "code": "#include <iostream>\n\nusing namespace std;\n\nbool isPerfectSquare(int num)\n{\n    long left = 1, right = num;\n\n    while (left <= right)\n    {\n        long mid = left + (right - left) / 2;\n        long square = mid * mid;\n\n        if (square == num) return true;\n        else if (square < num) left = mid + 1;\n        else right = mid - 1;\n    }\n\n    return false;\n}\n\nint main()\n{\n    cout << (isPerfectSquare(16) ? \"true\" : \"false\") << endl;\n\n    return 0;\n}",
          "output": "true",
          "timeComplexity": "O(log num)",
          "spaceComplexity": "O(1)"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-374",
      "number": 374,
      "title": "Guess Number Higher or Lower",
      "difficulty": "Easy",
      "link": "https://leetcode.com/problems/guess-number-higher-or-lower/",
      "problemStatement": "A number pick is chosen between 1 and n. A pre-built API guess(num) returns -1 (pick is lower), 1 (pick is higher), or 0 (correct). Find pick using the fewest calls.",
      "examples": [
        { "input": "n = 10, pick = 6", "output": "6" }
      ],
      "constraints": ["1 <= n <= 2^31 - 1"],
      "hints": [
        "This is binary search wearing a costume — the 'array' is just the range [1, n], and guess(mid) tells you which half pick is in, just like comparing to nums[mid] would.",
        "guess(mid) == -1 means pick is lower — search the left half. guess(mid) == 1 means search the right half.",
        "guess(mid) == 0 means mid IS pick — done."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Call guess() on every number from 1 to n until the correct one is found.",
          "code": "#include <iostream>\n\nusing namespace std;\n\nint pickedNumber = 6; // simulates the hidden pick for this demo\n\nint guess(int num)\n{\n    if (num > pickedNumber) return -1;\n    if (num < pickedNumber) return 1;\n    return 0;\n}\n\nint guessNumber(int n)\n{\n    for (int i = 1; i <= n; i++)\n    {\n        if (guess(i) == 0) return i;\n    }\n    return -1;\n}\n\nint main()\n{\n    cout << guessNumber(10) << endl;\n\n    return 0;\n}",
          "output": "6",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(1)"
        },
        {
          "label": "Optimal",
          "explanation": "Binary search over [1, n], using guess(mid)'s return value exactly like comparing to a sorted array's middle element.",
          "code": "#include <iostream>\n\nusing namespace std;\n\nint pickedNumber = 6; // simulates the hidden pick for this demo\n\nint guess(int num)\n{\n    if (num > pickedNumber) return -1;\n    if (num < pickedNumber) return 1;\n    return 0;\n}\n\nint guessNumber(int n)\n{\n    int left = 1, right = n;\n\n    while (left <= right)\n    {\n        int mid = left + (right - left) / 2;\n        int result = guess(mid);\n\n        if (result == 0) return mid;\n        else if (result == -1) right = mid - 1;\n        else left = mid + 1;\n    }\n\n    return -1;\n}\n\nint main()\n{\n    cout << guessNumber(10) << endl;\n\n    return 0;\n}",
          "output": "6",
          "timeComplexity": "O(log n)",
          "spaceComplexity": "O(1)"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-278",
      "number": 278,
      "title": "First Bad Version",
      "difficulty": "Easy",
      "link": "https://leetcode.com/problems/first-bad-version/",
      "problemStatement": "Versions 1 to n exist; once a version is bad, all following versions are also bad. Given an API isBadVersion(version), find the first bad version using as few calls as possible.",
      "examples": [
        { "input": "n = 5, bad = 4", "output": "4" }
      ],
      "constraints": ["1 <= bad <= n <= 2^31 - 1"],
      "hints": [
        "The versions form an implicit sorted sequence: good, good, ..., good, bad, bad, ..., bad — this is binary search for the BOUNDARY between two states, not for an exact value.",
        "isBadVersion(mid) == true means the first bad version is at mid or earlier — search left, but keep mid as a candidate answer.",
        "isBadVersion(mid) == false means the first bad version must be later — search right, discarding mid entirely."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Call isBadVersion() on every version starting from 1 until the first bad one is found.",
          "code": "#include <iostream>\n\nusing namespace std;\n\nint firstBad = 4; // simulates the hidden first bad version for this demo\n\nbool isBadVersion(int version)\n{\n    return version >= firstBad;\n}\n\nint firstBadVersion(int n)\n{\n    for (int i = 1; i <= n; i++)\n    {\n        if (isBadVersion(i)) return i;\n    }\n    return -1;\n}\n\nint main()\n{\n    cout << firstBadVersion(5) << endl;\n\n    return 0;\n}",
          "output": "4",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(1)"
        },
        {
          "label": "Optimal",
          "explanation": "Binary search for the boundary: when mid is bad, it's a candidate but a smaller bad version might still exist, so search left without discarding mid.",
          "code": "#include <iostream>\n\nusing namespace std;\n\nint firstBad = 4; // simulates the hidden first bad version for this demo\n\nbool isBadVersion(int version)\n{\n    return version >= firstBad;\n}\n\nint firstBadVersion(int n)\n{\n    int left = 1, right = n;\n\n    while (left < right)\n    {\n        int mid = left + (right - left) / 2;\n\n        if (isBadVersion(mid))\n        {\n            right = mid; // mid could be the answer, keep it in range\n        }\n        else\n        {\n            left = mid + 1;\n        }\n    }\n\n    return left;\n}\n\nint main()\n{\n    cout << firstBadVersion(5) << endl;\n\n    return 0;\n}",
          "output": "4",
          "timeComplexity": "O(log n)",
          "spaceComplexity": "O(1)"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-34",
      "number": 34,
      "title": "Find First and Last Position of Element in Sorted Array",
      "difficulty": "Medium",
      "link": "https://leetcode.com/problems/find-first-and-last-position-of-element-in-sorted-array/",
      "problemStatement": "Given a sorted array nums and a target, return [first, last] indices of target's occurrences, or [-1,-1] if not found, in O(log n) time.",
      "examples": [
        { "input": "nums = [5,7,7,8,8,10], target = 8", "output": "[3,4]" }
      ],
      "constraints": ["0 <= nums.length <= 10^5"],
      "hints": [
        "A single binary search finds AN occurrence of target, but not necessarily the first or last one when duplicates exist.",
        "Run TWO binary searches: one biased to keep searching left even after finding a match (to find the first occurrence), one biased to keep searching right (to find the last).",
        "This is First Bad Version's boundary-search idea, applied twice with opposite bias."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Scan linearly, recording the first and last index where nums[i] == target.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nvector<int> searchRange(vector<int> &nums, int target)\n{\n    int first = -1, last = -1;\n\n    for (int i = 0; i < nums.size(); i++)\n    {\n        if (nums[i] == target)\n        {\n            if (first == -1) first = i;\n            last = i;\n        }\n    }\n\n    return {first, last};\n}\n\nint main()\n{\n    vector<int> nums = {5, 7, 7, 8, 8, 10};\n\n    vector<int> result = searchRange(nums, 8);\n    cout << result[0] << \" \" << result[1] << endl;\n\n    return 0;\n}",
          "output": "3 4",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(1)"
        },
        {
          "label": "Optimal",
          "explanation": "Two binary searches: one for the leftmost occurrence (bias left on a match), one for the rightmost (bias right on a match).",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint findBound(vector<int> &nums, int target, bool findFirst)\n{\n    int left = 0, right = nums.size() - 1;\n    int result = -1;\n\n    while (left <= right)\n    {\n        int mid = left + (right - left) / 2;\n\n        if (nums[mid] == target)\n        {\n            result = mid;\n            if (findFirst) right = mid - 1; // keep searching left\n            else left = mid + 1;            // keep searching right\n        }\n        else if (nums[mid] < target) left = mid + 1;\n        else right = mid - 1;\n    }\n\n    return result;\n}\n\nvector<int> searchRange(vector<int> &nums, int target)\n{\n    return {findBound(nums, target, true), findBound(nums, target, false)};\n}\n\nint main()\n{\n    vector<int> nums = {5, 7, 7, 8, 8, 10};\n\n    vector<int> result = searchRange(nums, 8);\n    cout << result[0] << \" \" << result[1] << endl;\n\n    return 0;\n}",
          "output": "3 4",
          "timeComplexity": "O(log n)",
          "spaceComplexity": "O(1)"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-33",
      "number": 33,
      "title": "Search in Rotated Sorted Array",
      "difficulty": "Medium",
      "link": "https://leetcode.com/problems/search-in-rotated-sorted-array/",
      "problemStatement": "Given a sorted array that has been rotated at an unknown pivot, and a target, return its index, or -1 if not found, in O(log n) time.",
      "examples": [
        { "input": "nums = [4,5,6,7,0,1,2], target = 0", "output": "4" }
      ],
      "constraints": ["1 <= nums.length <= 5000"],
      "hints": [
        "The array isn't fully sorted anymore, but here's the key insight: at ANY mid point, at least ONE of the two halves (left of mid, or right of mid) IS still fully sorted.",
        "First figure out which half is sorted by comparing nums[left] to nums[mid].",
        "Once you know which half is sorted, checking whether target falls within THAT half's value range tells you which half to search next — same halving logic as normal binary search, just with an extra decision first."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Scan the array linearly, ignoring the rotation entirely.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint search(vector<int> &nums, int target)\n{\n    for (int i = 0; i < nums.size(); i++)\n    {\n        if (nums[i] == target) return i;\n    }\n    return -1;\n}\n\nint main()\n{\n    vector<int> nums = {4, 5, 6, 7, 0, 1, 2};\n\n    cout << search(nums, 0) << endl;\n\n    return 0;\n}",
          "output": "4",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(1)"
        },
        {
          "label": "Optimal",
          "explanation": "Modified binary search: identify the sorted half at each step, then decide which half to search based on whether target falls in that half's range.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint search(vector<int> &nums, int target)\n{\n    int left = 0, right = nums.size() - 1;\n\n    while (left <= right)\n    {\n        int mid = left + (right - left) / 2;\n\n        if (nums[mid] == target) return mid;\n\n        if (nums[left] <= nums[mid]) // left half is sorted\n        {\n            if (nums[left] <= target && target < nums[mid])\n            {\n                right = mid - 1;\n            }\n            else\n            {\n                left = mid + 1;\n            }\n        }\n        else // right half is sorted\n        {\n            if (nums[mid] < target && target <= nums[right])\n            {\n                left = mid + 1;\n            }\n            else\n            {\n                right = mid - 1;\n            }\n        }\n    }\n\n    return -1;\n}\n\nint main()\n{\n    vector<int> nums = {4, 5, 6, 7, 0, 1, 2};\n\n    cout << search(nums, 0) << endl;\n\n    return 0;\n}",
          "output": "4",
          "timeComplexity": "O(log n)",
          "spaceComplexity": "O(1)"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-153",
      "number": 153,
      "title": "Find Minimum in Rotated Sorted Array",
      "difficulty": "Medium",
      "link": "https://leetcode.com/problems/find-minimum-in-rotated-sorted-array/",
      "problemStatement": "Given a sorted array rotated at an unknown pivot, with no duplicate values, return the minimum element in O(log n) time.",
      "examples": [
        { "input": "nums = [3,4,5,1,2]", "output": "1" }
      ],
      "constraints": ["1 <= nums.length <= 5000", "all values unique"],
      "hints": [
        "The minimum is exactly the 'pivot point' where the rotation happened — everything before it is larger than everything after it.",
        "Comparing nums[mid] to nums[right] tells you which side the pivot is on: if nums[mid] > nums[right], the minimum must be somewhere to the right of mid; otherwise it's at mid or to the left.",
        "Unlike standard binary search, never discard mid itself when nums[mid] <= nums[right] — it could BE the minimum."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Scan the whole array tracking the minimum seen.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint findMin(vector<int> &nums)\n{\n    int minVal = nums[0];\n    for (int n : nums) minVal = min(minVal, n);\n    return minVal;\n}\n\nint main()\n{\n    vector<int> nums = {3, 4, 5, 1, 2};\n\n    cout << findMin(nums) << endl;\n\n    return 0;\n}",
          "output": "1",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(1)"
        },
        {
          "label": "Optimal",
          "explanation": "Binary search comparing nums[mid] to nums[right] to decide which half contains the rotation pivot (the minimum).",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint findMin(vector<int> &nums)\n{\n    int left = 0, right = nums.size() - 1;\n\n    while (left < right)\n    {\n        int mid = left + (right - left) / 2;\n\n        if (nums[mid] > nums[right])\n        {\n            left = mid + 1; // minimum is to the right\n        }\n        else\n        {\n            right = mid; // minimum is at mid or to the left\n        }\n    }\n\n    return nums[left];\n}\n\nint main()\n{\n    vector<int> nums = {3, 4, 5, 1, 2};\n\n    cout << findMin(nums) << endl;\n\n    return 0;\n}",
          "output": "1",
          "timeComplexity": "O(log n)",
          "spaceComplexity": "O(1)"
        }
      ]
    }
  ],
  "codeExamples": [
    { "id": "dsa-binary-search-basic", "lang": "cpp", "filename": "main.cpp", "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint binarySearch(vector<int> &numbers, int target)\n{\n    int left = 0;\n    int right = numbers.size() - 1;\n\n    while (left <= right)\n    {\n        int mid = left + (right - left) / 2;\n\n        if (numbers[mid] == target)\n        {\n            return mid;\n        }\n        else if (numbers[mid] < target)\n        {\n            left = mid + 1;\n        }\n        else\n        {\n            right = mid - 1;\n        }\n    }\n    return -1;\n}\n\nint main()\n{\n    vector<int> numbers = {2, 5, 8, 12, 16, 23, 38, 45};\n\n    cout << binarySearch(numbers, 23) << endl;\n\n    return 0;\n}", "output": "5" }
  ],
  "retypePractice": [
    {
      "id": "dsa-binary-search-basic",
      "reference": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint binarySearch(vector<int> &numbers, int target)\n{\n    int left = 0;\n    int right = numbers.size() - 1;\n\n    while (left <= right)\n    {\n        int mid = left + (right - left) / 2;\n\n        if (numbers[mid] == target)\n        {\n            return mid;\n        }\n        else if (numbers[mid] < target)\n        {\n            left = mid + 1;\n        }\n        else\n        {\n            right = mid - 1;\n        }\n    }\n    return -1;\n}\n\nint main()\n{\n    vector<int> numbers = {2, 5, 8, 12, 16, 23, 38, 45};\n\n    cout << binarySearch(numbers, 23) << endl;\n\n    return 0;\n}"
    }
  ],
  "practiceQuestions": [],
  "interviewQuestions": true,
  "leetcode": [],
  "completionRules": {
    "requireAllSectionsViewed": true,
    "requireRetypeIds": ["dsa-binary-search-basic"]
  }
}
