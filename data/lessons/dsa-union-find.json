{
  "id": "dsa-union-find",
  "moduleId": "graphs",
  "title": "Union Find / Disjoint Set",
  "description": "A different kind of tool entirely: quickly answering 'are these two vertices in the same group?' — with path compression, and cycle detection as its classic use.",
  "objectives": [
    "Understand the problem union find solves: tracking disjoint groups",
    "Implement find() with path compression",
    "Implement union() to merge two groups",
    "Use union find to detect a cycle in an undirected graph"
  ],
  "sections": [
    {
      "type": "heading",
      "text": "1. Recap: A New Kind of Question"
    },
    {
      "type": "paragraph",
      "text": "BFS and DFS answer \"how do I reach every vertex from here?\" Union find answers a different, narrower question extremely efficiently: \"are these two specific vertices already connected, directly or indirectly?\""
    },
    {
      "type": "heading",
      "text": "2. What is Union Find?"
    },
    {
      "type": "definition",
      "term": "Union find (disjoint set)",
      "text": "Union find is a data structure that tracks a collection of disjoint (non-overlapping) groups, supporting two operations: find (which group is this element in?) and union (merge two groups into one)."
    },
    {
      "type": "heading",
      "text": "3. The Structure: A parent Array"
    },
    {
      "type": "code",
      "lang": "cpp",
      "code": "vector<int> parent(n);\nfor (int i = 0; i < n; i++)\n{\n    parent[i] = i; // everyone starts as their OWN group's root\n}"
    },
    {
      "type": "note",
      "text": "Each element points to a \"parent\" — following parent pointers upward eventually reaches a root, which represents that entire group. Initially, everyone is their own root, meaning n separate groups."
    },
    {
      "type": "heading",
      "text": "4. find() — With Path Compression"
    },
    {
      "type": "code",
      "lang": "cpp",
      "code": "int find(vector<int> &parent, int x)\n{\n    if (parent[x] != x)\n    {\n        parent[x] = find(parent, parent[x]); // PATH COMPRESSION — flatten the chain as we go\n    }\n    return parent[x];\n}"
    },
    {
      "type": "definition",
      "term": "Path compression",
      "text": "Path compression makes every node visited during a find() point directly to the root, so future find() calls on those same nodes become nearly instant."
    },
    {
      "type": "heading",
      "text": "5. Full Example: union() and Cycle Detection"
    },
    {
      "type": "code",
      "lang": "cpp",
      "filename": "main.cpp",
      "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint find(vector<int> &parent, int x)\n{\n    if (parent[x] != x)\n    {\n        parent[x] = find(parent, parent[x]);\n    }\n    return parent[x];\n}\n\nbool unite(vector<int> &parent, int a, int b)\n{\n    int rootA = find(parent, a);\n    int rootB = find(parent, b);\n\n    if (rootA == rootB)\n    {\n        return false; // already connected — adding this edge would create a CYCLE\n    }\n\n    parent[rootA] = rootB; // merge the two groups\n    return true;\n}\n\nint main()\n{\n    int n = 5;\n    vector<int> parent(n);\n    for (int i = 0; i < n; i++) parent[i] = i;\n\n    vector<pair<int, int>> edges = {{0, 1}, {1, 2}, {3, 4}, {0, 2}};\n\n    for (auto &edge : edges)\n    {\n        if (unite(parent, edge.first, edge.second))\n        {\n            cout << \"Union: \" << edge.first << \"-\" << edge.second << endl;\n        }\n        else\n        {\n            cout << \"Cycle detected: \" << edge.first << \"-\" << edge.second << endl;\n        }\n    }\n\n    return 0;\n}",
      "output": "Union: 0-1\nUnion: 1-2\nUnion: 3-4\nCycle detected: 0-2",
      "retypePractice": true,
      "retypeId": "dsa-union-find-basic"
    },
    {
      "type": "keyPoints",
      "title": "Why the last edge is flagged",
      "points": [
        "After processing (0,1) and (1,2), vertices 0, 1, and 2 all belong to the SAME group (connected, directly or indirectly)",
        "(3,4) forms a completely separate second group",
        "When (0,2) is processed, find(0) and find(2) return the SAME root — they're already connected",
        "Adding an edge between two vertices already in the same group would create a cycle, exactly what unite() correctly detects and reports"
      ]
    },
    {
      "type": "heading",
      "text": "6. Real-Life Example"
    },
    {
      "type": "realLifeExample",
      "title": "Merging friend groups, and noticing when two people are already in the same one",
      "text": "Imagine tracking separate friend groups as people get introduced to each other. Introducing two people from entirely different groups merges those groups into one. But if someone tries to \"introduce\" two people who turn out to already be in the same friend group (through some other chain of mutual friends), that's not a new connection at all — it's redundant, exactly like a cycle in a graph."
    },
    {
      "type": "commonMistakes",
      "title": "Common Beginner Mistakes",
      "items": [
        "Forgetting path compression — find() still works without it, but degrades toward O(n) per call on a long chain instead of staying nearly O(1).",
        "Comparing a and b directly instead of comparing find(a) and find(b) — two elements in the same group don't necessarily have the same immediate parent, only the same ROOT.",
        "Assuming union find can answer 'what is the shortest path between these two' — it can only answer 'are they connected at all,' nothing about the path itself.",
        "Forgetting to initialize parent[i] = i for every element before use — an uninitialized parent array doesn't represent 'everyone is their own group' correctly."
      ]
    },
    {
      "type": "interviewQuestions",
      "title": "Interview Questions",
      "questions": [
        {
          "question": "What problem does union find solve?",
          "answer": "Efficiently tracking a collection of disjoint groups, answering 'are these two elements in the same group' and merging two groups together."
        },
        {
          "question": "What does path compression do in find()?",
          "answer": "It makes every node visited during the find() call point directly to the root, so future find() calls on those nodes become nearly instant."
        },
        {
          "question": "How can union find be used to detect a cycle in an undirected graph?",
          "answer": "Process each edge with union(); if the two vertices already share the same root (are already connected) before the edge is added, that edge would create a cycle."
        }
      ],
      "thirtySecondAnswer": "Union find tracks disjoint groups using a parent array, where each element points toward a root representing its group. find(x) follows parent pointers up to the root, applying path compression along the way so future calls become nearly instant. union(a, b) merges two groups by connecting their roots — but if find(a) and find(b) already return the same root, they're already connected, and forcing that edge would create a cycle, which is exactly how union find is used to detect cycles while building an undirected graph edge by edge."
    },
    {
      "type": "memoryTrick",
      "title": "Trick to Remember Union Find",
      "text": "Union find = merging friend groups. find() = which group's root am I part of? union() = merge two groups. Same root already? That new edge is a cycle."
    },
    {
      "type": "heading",
      "text": "LeetCode: Union Find in Practice"
    },
    {
      "type": "leetcode",
      "id": "lc-547-uf",
      "number": 547,
      "title": "Number of Provinces",
      "difficulty": "Medium",
      "link": "https://leetcode.com/problems/number-of-provinces/",
      "problemStatement": "Given an n x n adjacency matrix isConnected, return the number of provinces (connected groups of cities).",
      "examples": [
        { "input": "isConnected = [[1,1,0],[1,1,0],[0,0,1]]", "output": "2" }
      ],
      "constraints": ["1 <= n <= 200"],
      "hints": [
        "This lesson's own technique, applied directly: union() every pair of directly-connected cities, then count how many distinct roots remain.",
        "No traversal needed at all — just iterate the matrix once, calling union(i, j) wherever isConnected[i][j] == 1.",
        "The final province count is the number of cities i where find(i) == i (i.e., i is its own root)."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Union find WITHOUT path compression or union by rank — find() walks the raw parent chain every time.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint find(int x, vector<int>& parent)\n{\n    while (parent[x] != x) x = parent[x];\n    return x;\n}\n\nvoid unite(int a, int b, vector<int>& parent)\n{\n    int rootA = find(a, parent);\n    int rootB = find(b, parent);\n    if (rootA != rootB) parent[rootA] = rootB;\n}\n\nint findCircleNum(vector<vector<int>>& isConnected)\n{\n    int n = isConnected.size();\n    vector<int> parent(n);\n    for (int i = 0; i < n; i++) parent[i] = i;\n\n    for (int i = 0; i < n; i++)\n        for (int j = i + 1; j < n; j++)\n            if (isConnected[i][j] == 1) unite(i, j, parent);\n\n    int provinces = 0;\n    for (int i = 0; i < n; i++)\n        if (find(i, parent) == i) provinces++;\n\n    return provinces;\n}\n\nint main()\n{\n    vector<vector<int>> isConnected = {\n        {1, 1, 0},\n        {1, 1, 0},\n        {0, 0, 1}\n    };\n\n    cout << findCircleNum(isConnected);\n\n    return 0;\n}",
          "output": "2",
          "timeComplexity": "O(n^2 * n) worst case without path compression — a degenerate chain makes find() O(n)",
          "spaceComplexity": "O(n) for the parent array"
        },
        {
          "label": "Optimal",
          "explanation": "Union find with path compression — find() flattens the chain directly to the root, making future calls near-instant.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint find(int x, vector<int>& parent)\n{\n    if (parent[x] != x) parent[x] = find(parent[x], parent); // path compression\n    return parent[x];\n}\n\nvoid unite(int a, int b, vector<int>& parent)\n{\n    int rootA = find(a, parent);\n    int rootB = find(b, parent);\n    if (rootA != rootB) parent[rootA] = rootB;\n}\n\nint findCircleNum(vector<vector<int>>& isConnected)\n{\n    int n = isConnected.size();\n    vector<int> parent(n);\n    for (int i = 0; i < n; i++) parent[i] = i;\n\n    for (int i = 0; i < n; i++)\n        for (int j = i + 1; j < n; j++)\n            if (isConnected[i][j] == 1) unite(i, j, parent);\n\n    int provinces = 0;\n    for (int i = 0; i < n; i++)\n        if (find(i, parent) == i) provinces++;\n\n    return provinces;\n}\n\nint main()\n{\n    vector<vector<int>> isConnected = {\n        {1, 1, 0},\n        {1, 1, 0},\n        {0, 0, 1}\n    };\n\n    cout << findCircleNum(isConnected);\n\n    return 0;\n}",
          "output": "2",
          "timeComplexity": "O(n^2 * α(n)) — near-constant find() amortized with path compression",
          "spaceComplexity": "O(n) for the parent array"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-684",
      "number": 684,
      "title": "Redundant Connection",
      "difficulty": "Medium",
      "link": "https://leetcode.com/problems/redundant-connection/",
      "problemStatement": "A tree with n nodes had one extra edge added, creating exactly one cycle. Given the edges in input order, return the edge that can be removed to make it a tree again — if multiple answers exist, return the one that appears LAST in the input.",
      "examples": [
        { "input": "edges = [[1,2],[1,3],[2,3]]", "output": "[2,3]" }
      ],
      "constraints": ["3 <= edges.length <= 1000"],
      "hints": [
        "This is exactly this lesson's cycle-detection use case: process edges in order, union() each one, and the first edge where find(a) == find(b) BEFORE unioning is the answer.",
        "'Last edge to appear in the input' falls out naturally — just process edges in their given order and return the FIRST one union() reports as a cycle (there's only one, by the problem's guarantee).",
        "No traversal is needed at all — this is purely find()/union() calls in a single pass, no BFS/DFS."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Union find without path compression — find() walks the raw chain, which can degrade on a long unlucky merge order.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint find(int x, vector<int>& parent)\n{\n    while (parent[x] != x) x = parent[x];\n    return x;\n}\n\nvector<int> findRedundantConnection(vector<vector<int>>& edges)\n{\n    int n = edges.size();\n    vector<int> parent(n + 1);\n    for (int i = 0; i <= n; i++) parent[i] = i;\n\n    for (auto &edge : edges)\n    {\n        int rootA = find(edge[0], parent);\n        int rootB = find(edge[1], parent);\n        if (rootA == rootB) return edge;\n        parent[rootA] = rootB;\n    }\n    return {};\n}\n\nint main()\n{\n    vector<vector<int>> edges = {{1, 2}, {1, 3}, {2, 3}};\n    vector<int> result = findRedundantConnection(edges);\n    cout << \"[\" << result[0] << \",\" << result[1] << \"]\";\n\n    return 0;\n}",
          "output": "[2,3]",
          "timeComplexity": "O(n^2) worst case without path compression",
          "spaceComplexity": "O(n) for the parent array"
        },
        {
          "label": "Optimal",
          "explanation": "Union find with path compression — near-constant find() per edge.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint find(int x, vector<int>& parent)\n{\n    if (parent[x] != x) parent[x] = find(parent[x], parent);\n    return parent[x];\n}\n\nvector<int> findRedundantConnection(vector<vector<int>>& edges)\n{\n    int n = edges.size();\n    vector<int> parent(n + 1);\n    for (int i = 0; i <= n; i++) parent[i] = i;\n\n    for (auto &edge : edges)\n    {\n        int rootA = find(edge[0], parent);\n        int rootB = find(edge[1], parent);\n        if (rootA == rootB) return edge;\n        parent[rootA] = rootB;\n    }\n    return {};\n}\n\nint main()\n{\n    vector<vector<int>> edges = {{1, 2}, {1, 3}, {2, 3}};\n    vector<int> result = findRedundantConnection(edges);\n    cout << \"[\" << result[0] << \",\" << result[1] << \"]\";\n\n    return 0;\n}",
          "output": "[2,3]",
          "timeComplexity": "O(n * α(n)) — near-constant find() amortized with path compression",
          "spaceComplexity": "O(n) for the parent array"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-1971-uf",
      "number": 1971,
      "title": "Find if Path Exists in Graph",
      "difficulty": "Easy",
      "link": "https://leetcode.com/problems/find-if-path-exists-in-graph/",
      "problemStatement": "Given an undirected graph as a list of edges, and two vertices source and destination, return true if there is a path between them.",
      "examples": [
        { "input": "n = 3, edges = [[0,1],[1,2],[2,0]], source = 0, destination = 2", "output": "true" }
      ],
      "constraints": ["1 <= n <= 2 * 10^5", "0 <= edges.length <= 2 * 10^5"],
      "hints": [
        "Instead of BFS/DFS, union() every edge first, then just compare find(source) and find(destination) — same root means connected.",
        "This trades traversal for the union find structure this lesson builds — a clean 'are these two connected at all' fit, with no need to reconstruct a path.",
        "For a single static query like this, BFS/DFS work just as well; union find shines more when checking MANY connectivity queries against a fixed or growing edge set."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Union find without path compression.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint find(int x, vector<int>& parent)\n{\n    while (parent[x] != x) x = parent[x];\n    return x;\n}\n\nbool validPath(int n, vector<vector<int>>& edges, int source, int destination)\n{\n    vector<int> parent(n);\n    for (int i = 0; i < n; i++) parent[i] = i;\n\n    for (auto &e : edges)\n    {\n        int rootA = find(e[0], parent);\n        int rootB = find(e[1], parent);\n        if (rootA != rootB) parent[rootA] = rootB;\n    }\n\n    return find(source, parent) == find(destination, parent);\n}\n\nint main()\n{\n    vector<vector<int>> edges = {{0, 1}, {1, 2}, {2, 0}};\n    cout << boolalpha << validPath(3, edges, 0, 2);\n\n    return 0;\n}",
          "output": "true",
          "timeComplexity": "O(edges * n) worst case without path compression",
          "spaceComplexity": "O(n) for the parent array"
        },
        {
          "label": "Optimal",
          "explanation": "Union find with path compression.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint find(int x, vector<int>& parent)\n{\n    if (parent[x] != x) parent[x] = find(parent[x], parent);\n    return parent[x];\n}\n\nbool validPath(int n, vector<vector<int>>& edges, int source, int destination)\n{\n    vector<int> parent(n);\n    for (int i = 0; i < n; i++) parent[i] = i;\n\n    for (auto &e : edges)\n    {\n        int rootA = find(e[0], parent);\n        int rootB = find(e[1], parent);\n        if (rootA != rootB) parent[rootA] = rootB;\n    }\n\n    return find(source, parent) == find(destination, parent);\n}\n\nint main()\n{\n    vector<vector<int>> edges = {{0, 1}, {1, 2}, {2, 0}};\n    cout << boolalpha << validPath(3, edges, 0, 2);\n\n    return 0;\n}",
          "output": "true",
          "timeComplexity": "O(edges * α(n)) — near-constant find() amortized with path compression",
          "spaceComplexity": "O(n) for the parent array"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-721",
      "number": 721,
      "title": "Accounts Merge",
      "difficulty": "Medium",
      "link": "https://leetcode.com/problems/accounts-merge/",
      "problemStatement": "Given a list of accounts, each [name, email1, email2, ...], merge accounts that share at least one email (transitively), and return each merged account as [name, sorted emails...].",
      "examples": [
        { "input": "accounts = [[\"John\",\"johnsmith@mail.com\",\"john_newyork@mail.com\"],[\"John\",\"johnsmith@mail.com\",\"john00@mail.com\"],[\"Mary\",\"mary@mail.com\"],[\"John\",\"johnny@mail.com\"]]", "output": "[[\"John\",\"john00@mail.com\",\"john_newyork@mail.com\",\"johnsmith@mail.com\"],[\"Mary\",\"mary@mail.com\"],[\"John\",\"johnny@mail.com\"]]" }
      ],
      "constraints": ["1 <= accounts.length <= 1000"],
      "hints": [
        "Union find here operates on EMAILS as elements, not account indices — two emails in the same account get union()'d together.",
        "Since 'John' appears in multiple unrelated accounts, only shared EMAILS (not shared names) determine which accounts truly merge.",
        "After all unions, group every email by its root, sort each group's emails, and prepend the owner's name (any account containing that root's emails has the right name)."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Union find on emails using string keys, without path compression.",
          "code": "#include <iostream>\n#include <vector>\n#include <string>\n#include <unordered_map>\n#include <algorithm>\n\nusing namespace std;\n\nstring find(string x, unordered_map<string, string>& parent)\n{\n    while (parent[x] != x) x = parent[x];\n    return x;\n}\n\nvoid unite(string a, string b, unordered_map<string, string>& parent)\n{\n    string rootA = find(a, parent);\n    string rootB = find(b, parent);\n    if (rootA != rootB) parent[rootA] = rootB;\n}\n\nvector<vector<string>> accountsMerge(vector<vector<string>>& accounts)\n{\n    unordered_map<string, string> parent;\n    unordered_map<string, string> emailToName;\n    vector<string> emailOrder;\n\n    for (auto &account : accounts)\n    {\n        string name = account[0];\n        for (int i = 1; i < (int)account.size(); i++)\n        {\n            string email = account[i];\n            if (parent.find(email) == parent.end())\n            {\n                parent[email] = email;\n                emailOrder.push_back(email);\n            }\n            emailToName[email] = name;\n            if (i > 1) unite(account[1], email, parent);\n        }\n    }\n\n    unordered_map<string, vector<string>> groups;\n    vector<string> rootOrder;\n    for (auto &email : emailOrder)\n    {\n        string root = find(email, parent);\n        if (groups.find(root) == groups.end()) rootOrder.push_back(root);\n        groups[root].push_back(email);\n    }\n\n    vector<vector<string>> result;\n    for (auto &root : rootOrder)\n    {\n        vector<string> emails = groups[root];\n        sort(emails.begin(), emails.end());\n        vector<string> entry = {emailToName[root]};\n        for (auto &e : emails) entry.push_back(e);\n        result.push_back(entry);\n    }\n    return result;\n}\n\nint main()\n{\n    vector<vector<string>> accounts = {\n        {\"John\", \"johnsmith@mail.com\", \"john_newyork@mail.com\"},\n        {\"John\", \"johnsmith@mail.com\", \"john00@mail.com\"},\n        {\"Mary\", \"mary@mail.com\"},\n        {\"John\", \"johnny@mail.com\"}\n    };\n\n    vector<vector<string>> result = accountsMerge(accounts);\n    for (auto &entry : result)\n    {\n        for (auto &s : entry) cout << s << \" \";\n        cout << \"| \";\n    }\n\n    return 0;\n}",
          "output": "John john00@mail.com john_newyork@mail.com johnsmith@mail.com | Mary mary@mail.com | John johnny@mail.com | ",
          "timeComplexity": "O(n log n) for sorting emails per group, with O(n) per find() worst case without path compression",
          "spaceComplexity": "O(n) for the parent map and grouping structures"
        },
        {
          "label": "Optimal",
          "explanation": "Same structure, with path compression on the string-keyed parent map for near-constant find() calls.",
          "code": "#include <iostream>\n#include <vector>\n#include <string>\n#include <unordered_map>\n#include <algorithm>\n\nusing namespace std;\n\nstring find(string x, unordered_map<string, string>& parent)\n{\n    if (parent[x] != x) parent[x] = find(parent[x], parent);\n    return parent[x];\n}\n\nvoid unite(string a, string b, unordered_map<string, string>& parent)\n{\n    string rootA = find(a, parent);\n    string rootB = find(b, parent);\n    if (rootA != rootB) parent[rootA] = rootB;\n}\n\nvector<vector<string>> accountsMerge(vector<vector<string>>& accounts)\n{\n    unordered_map<string, string> parent;\n    unordered_map<string, string> emailToName;\n    vector<string> emailOrder;\n\n    for (auto &account : accounts)\n    {\n        string name = account[0];\n        for (int i = 1; i < (int)account.size(); i++)\n        {\n            string email = account[i];\n            if (parent.find(email) == parent.end())\n            {\n                parent[email] = email;\n                emailOrder.push_back(email);\n            }\n            emailToName[email] = name;\n            if (i > 1) unite(account[1], email, parent);\n        }\n    }\n\n    unordered_map<string, vector<string>> groups;\n    vector<string> rootOrder;\n    for (auto &email : emailOrder)\n    {\n        string root = find(email, parent);\n        if (groups.find(root) == groups.end()) rootOrder.push_back(root);\n        groups[root].push_back(email);\n    }\n\n    vector<vector<string>> result;\n    for (auto &root : rootOrder)\n    {\n        vector<string> emails = groups[root];\n        sort(emails.begin(), emails.end());\n        vector<string> entry = {emailToName[root]};\n        for (auto &e : emails) entry.push_back(e);\n        result.push_back(entry);\n    }\n    return result;\n}\n\nint main()\n{\n    vector<vector<string>> accounts = {\n        {\"John\", \"johnsmith@mail.com\", \"john_newyork@mail.com\"},\n        {\"John\", \"johnsmith@mail.com\", \"john00@mail.com\"},\n        {\"Mary\", \"mary@mail.com\"},\n        {\"John\", \"johnny@mail.com\"}\n    };\n\n    vector<vector<string>> result = accountsMerge(accounts);\n    for (auto &entry : result)\n    {\n        for (auto &s : entry) cout << s << \" \";\n        cout << \"| \";\n    }\n\n    return 0;\n}",
          "output": "John john00@mail.com john_newyork@mail.com johnsmith@mail.com | Mary mary@mail.com | John johnny@mail.com | ",
          "timeComplexity": "O(n log n) for sorting, with near-constant find() amortized via path compression",
          "spaceComplexity": "O(n) for the parent map and grouping structures"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-1319",
      "number": 1319,
      "title": "Number of Operations to Make Network Connected",
      "difficulty": "Medium",
      "link": "https://leetcode.com/problems/number-of-operations-to-make-network-connected/",
      "problemStatement": "Given n computers and connections (each a cable between two computers), return the minimum number of cable moves needed to connect all computers, or -1 if there aren't enough cables.",
      "examples": [
        { "input": "n = 4, connections = [[0,1],[0,2],[1,2]]", "output": "1" }
      ],
      "constraints": ["1 <= n <= 10^5"],
      "hints": [
        "A tree connecting n nodes needs exactly n-1 edges; fewer than that, and there simply aren't enough cables to connect everyone, regardless of arrangement — return -1 immediately.",
        "Otherwise, union() every connection, then count the number of distinct roots (components) — using this lesson's exact 'count roots' pattern from LC 547.",
        "Each redundant cable (one that unite() reports as already-connected) can be moved to bridge exactly one pair of separate components — so the answer is simply (components - 1)."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Union find without path compression, then count roots.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint find(int x, vector<int>& parent)\n{\n    while (parent[x] != x) x = parent[x];\n    return x;\n}\n\nint makeConnected(int n, vector<vector<int>>& connections)\n{\n    if ((int)connections.size() < n - 1) return -1;\n\n    vector<int> parent(n);\n    for (int i = 0; i < n; i++) parent[i] = i;\n\n    for (auto &c : connections)\n    {\n        int rootA = find(c[0], parent);\n        int rootB = find(c[1], parent);\n        if (rootA != rootB) parent[rootA] = rootB;\n    }\n\n    int components = 0;\n    for (int i = 0; i < n; i++)\n        if (find(i, parent) == i) components++;\n\n    return components - 1;\n}\n\nint main()\n{\n    vector<vector<int>> connections = {{0, 1}, {0, 2}, {1, 2}};\n    cout << makeConnected(4, connections);\n\n    return 0;\n}",
          "output": "1",
          "timeComplexity": "O((n + connections) * n) worst case without path compression",
          "spaceComplexity": "O(n) for the parent array"
        },
        {
          "label": "Optimal",
          "explanation": "Union find with path compression, then count roots.",
          "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint find(int x, vector<int>& parent)\n{\n    if (parent[x] != x) parent[x] = find(parent[x], parent);\n    return parent[x];\n}\n\nint makeConnected(int n, vector<vector<int>>& connections)\n{\n    if ((int)connections.size() < n - 1) return -1;\n\n    vector<int> parent(n);\n    for (int i = 0; i < n; i++) parent[i] = i;\n\n    for (auto &c : connections)\n    {\n        int rootA = find(c[0], parent);\n        int rootB = find(c[1], parent);\n        if (rootA != rootB) parent[rootA] = rootB;\n    }\n\n    int components = 0;\n    for (int i = 0; i < n; i++)\n        if (find(i, parent) == i) components++;\n\n    return components - 1;\n}\n\nint main()\n{\n    vector<vector<int>> connections = {{0, 1}, {0, 2}, {1, 2}};\n    cout << makeConnected(4, connections);\n\n    return 0;\n}",
          "output": "1",
          "timeComplexity": "O((n + connections) * α(n)) — near-constant find() amortized with path compression",
          "spaceComplexity": "O(n) for the parent array"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-1584",
      "number": 1584,
      "title": "Min Cost to Connect All Points",
      "difficulty": "Medium",
      "link": "https://leetcode.com/problems/min-cost-to-connect-all-points/",
      "problemStatement": "Given points on a 2D plane, connect all points with edges costing the Manhattan distance between them, minimizing total cost so every point is reachable from every other.",
      "examples": [
        { "input": "points = [[0,0],[2,2],[3,10],[5,2],[7,0]]", "output": "20" }
      ],
      "constraints": ["1 <= points.length <= 1000"],
      "hints": [
        "This is Minimum Spanning Tree (MST) territory — Kruskal's algorithm: sort ALL possible edges by weight, then greedily add the cheapest edge that doesn't create a cycle.",
        "Union find is exactly the cycle-check Kruskal's needs — union(a, b) succeeding means the edge connects two different components (keep it); failing means a cycle (skip it).",
        "Stop once exactly n-1 edges have been added — that's a spanning tree connecting all n points, and Kruskal's greedy edge order guarantees it's the minimum-cost one."
      ],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Kruskal's MST with union find WITHOUT path compression.",
          "code": "#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <cmath>\n#include <array>\n\nusing namespace std;\n\nint find(int x, vector<int>& parent)\n{\n    while (parent[x] != x) x = parent[x];\n    return x;\n}\n\nint minCostConnectPoints(vector<vector<int>>& points)\n{\n    int n = points.size();\n    vector<array<int, 3>> edges;\n\n    for (int i = 0; i < n; i++)\n        for (int j = i + 1; j < n; j++)\n        {\n            int cost = abs(points[i][0] - points[j][0]) + abs(points[i][1] - points[j][1]);\n            edges.push_back({cost, i, j});\n        }\n\n    sort(edges.begin(), edges.end());\n\n    vector<int> parent(n);\n    for (int i = 0; i < n; i++) parent[i] = i;\n\n    int totalCost = 0, edgesUsed = 0;\n    for (auto &edge : edges)\n    {\n        if (edgesUsed == n - 1) break;\n        int rootA = find(edge[1], parent);\n        int rootB = find(edge[2], parent);\n        if (rootA != rootB)\n        {\n            parent[rootA] = rootB;\n            totalCost += edge[0];\n            edgesUsed++;\n        }\n    }\n\n    return totalCost;\n}\n\nint main()\n{\n    vector<vector<int>> points = {{0, 0}, {2, 2}, {3, 10}, {5, 2}, {7, 0}};\n    cout << minCostConnectPoints(points);\n\n    return 0;\n}",
          "output": "20",
          "timeComplexity": "O(n^2 log n) — dominated by sorting all O(n^2) possible edges",
          "spaceComplexity": "O(n^2) for storing every possible edge"
        },
        {
          "label": "Optimal",
          "explanation": "Same Kruskal's approach, with path compression in union find for faster find() calls.",
          "code": "#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <cmath>\n#include <array>\n\nusing namespace std;\n\nint find(int x, vector<int>& parent)\n{\n    if (parent[x] != x) parent[x] = find(parent[x], parent);\n    return parent[x];\n}\n\nint minCostConnectPoints(vector<vector<int>>& points)\n{\n    int n = points.size();\n    vector<array<int, 3>> edges;\n\n    for (int i = 0; i < n; i++)\n        for (int j = i + 1; j < n; j++)\n        {\n            int cost = abs(points[i][0] - points[j][0]) + abs(points[i][1] - points[j][1]);\n            edges.push_back({cost, i, j});\n        }\n\n    sort(edges.begin(), edges.end());\n\n    vector<int> parent(n);\n    for (int i = 0; i < n; i++) parent[i] = i;\n\n    int totalCost = 0, edgesUsed = 0;\n    for (auto &edge : edges)\n    {\n        if (edgesUsed == n - 1) break;\n        int rootA = find(edge[1], parent);\n        int rootB = find(edge[2], parent);\n        if (rootA != rootB)\n        {\n            parent[rootA] = rootB;\n            totalCost += edge[0];\n            edgesUsed++;\n        }\n    }\n\n    return totalCost;\n}\n\nint main()\n{\n    vector<vector<int>> points = {{0, 0}, {2, 2}, {3, 10}, {5, 2}, {7, 0}};\n    cout << minCostConnectPoints(points);\n\n    return 0;\n}",
          "output": "20",
          "timeComplexity": "O(n^2 log n) — still dominated by sorting, but find() is near-constant amortized",
          "spaceComplexity": "O(n^2) for storing every possible edge"
        }
      ]
    }
  ],
  "codeExamples": [
    { "id": "dsa-union-find-basic", "lang": "cpp", "filename": "main.cpp", "code": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint find(vector<int> &parent, int x)\n{\n    if (parent[x] != x)\n    {\n        parent[x] = find(parent, parent[x]);\n    }\n    return parent[x];\n}\n\nbool unite(vector<int> &parent, int a, int b)\n{\n    int rootA = find(parent, a);\n    int rootB = find(parent, b);\n\n    if (rootA == rootB)\n    {\n        return false;\n    }\n\n    parent[rootA] = rootB;\n    return true;\n}\n\nint main()\n{\n    int n = 5;\n    vector<int> parent(n);\n    for (int i = 0; i < n; i++) parent[i] = i;\n\n    vector<pair<int, int>> edges = {{0, 1}, {1, 2}, {3, 4}, {0, 2}};\n\n    for (auto &edge : edges)\n    {\n        if (unite(parent, edge.first, edge.second))\n        {\n            cout << \"Union: \" << edge.first << \"-\" << edge.second << endl;\n        }\n        else\n        {\n            cout << \"Cycle detected: \" << edge.first << \"-\" << edge.second << endl;\n        }\n    }\n\n    return 0;\n}", "output": "Union: 0-1\nUnion: 1-2\nUnion: 3-4\nCycle detected: 0-2" }
  ],
  "retypePractice": [
    {
      "id": "dsa-union-find-basic",
      "reference": "#include <iostream>\n#include <vector>\n\nusing namespace std;\n\nint find(vector<int> &parent, int x)\n{\n    if (parent[x] != x)\n    {\n        parent[x] = find(parent, parent[x]);\n    }\n    return parent[x];\n}\n\nbool unite(vector<int> &parent, int a, int b)\n{\n    int rootA = find(parent, a);\n    int rootB = find(parent, b);\n\n    if (rootA == rootB)\n    {\n        return false;\n    }\n\n    parent[rootA] = rootB;\n    return true;\n}\n\nint main()\n{\n    int n = 5;\n    vector<int> parent(n);\n    for (int i = 0; i < n; i++) parent[i] = i;\n\n    vector<pair<int, int>> edges = {{0, 1}, {1, 2}, {3, 4}, {0, 2}};\n\n    for (auto &edge : edges)\n    {\n        if (unite(parent, edge.first, edge.second))\n        {\n            cout << \"Union: \" << edge.first << \"-\" << edge.second << endl;\n        }\n        else\n        {\n            cout << \"Cycle detected: \" << edge.first << \"-\" << edge.second << endl;\n        }\n    }\n\n    return 0;\n}"
    }
  ],
  "practiceQuestions": [],
  "interviewQuestions": true,
  "leetcode": [],
  "completionRules": {
    "requireAllSectionsViewed": true,
    "requireRetypeIds": ["dsa-union-find-basic"]
  }
}
